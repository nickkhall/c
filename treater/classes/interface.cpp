#include <vector>
#include <string>

Interface::Interface(std::vector<std::string> inputs, std::vector<std::string> labels)
  : inputs { inputs }, labels: { labels } {
};

void Interface::HandleInput() {

}
